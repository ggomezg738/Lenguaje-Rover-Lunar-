RoverLunar{
    var distanciaFinal = 100;
    var distanciaActual = 0;

    var foto = 0;
    var mensaje;

    girar(brazoA,izquierda,20);
    girar(brazoB,izquierda,20);
    
    desplazar(adelante,0.5);

    girar(brazoA,derecha,20); 
    girar(brazoB,derecha,20); 

    extender(pedestal,0.5); 
    girar(pedestal,derecha,70); 

    var i;
    repetir mientras (distanciaActual < distanciaFinal){
        i = 0;
        repetir mientras (i<3){
            tomarCaptura;
            foto = foto + 1;
            girar(pedestal,izquierda,70); 
            i = i+1;
        };
        girar(pedestal,derecha,140);
        desplazar(adelante,10);
        distanciaActual = distanciaActual + 10;
    };

    si (foto==10) entonces {
        mensaje = Exito;
    };
}@